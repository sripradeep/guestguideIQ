**Collaborator:** aidlc-developer-agent

## Contribution

Round-2 delta review only — scoped to the locality-branding addition folded into the already-approved `stories.md`/`personas.md` (AC1.1.4 + its new US1.1→US4.4 dependency, AC1.5.3, AC2.1.4, AC2.2.3, new story US4.4, and the new FR9/NFR6 rows in `traceability.json`). I did not re-review anything outside this delta; my Round-1 findings (all resolved per the product-lead's Review section) stand as-is.

Checked, from an implementability lens:

1. **US1.1 ↔ US4.4 dependency, both directions.** US1.1's Dependencies line ("US4.4 (a locality-brand must exist before signup through its domain is possible)") and US4.4's Priority rationale ("a precondition for US1.1's domain-based signup — no Property Owner can sign up through a locality-brand that doesn't exist yet") name each other correctly and agree on the direction of the constraint (US4.4 must be built/seeded before US1.1's domain-based path can be exercised). US4.4's own Dependencies line is correctly `None` — it has no upstream story dependency of its own. This is internally consistent.

2. **AC2.1.4 vs. NFR6.1 — MVP-vs-later split.** AC1.1.4 (domain-based signup) and AC2.1.4 (guest link resolves through the property's own locality-brand domain) both fall inside the walking-skeleton slice (US1.1, US1.5, US2.1 — Q1's answer A). But the mechanism that actually makes domain-based resolution work — NFR6.1 ("correctly resolve which locality-brand applies to an incoming request based on its domain") and NFR6.2 (TLS/certificate provisioning for an arbitrary number of custom domains) — is explicitly deferred to `domain-design`/`infrastructure-design` in `traceability.json`. That leaves an unresolved question of how much multi-domain routing the walking-skeleton Bolt is actually expected to stand up: proving AC1.1.4/AC2.1.4 end-to-end needs *some* working domain/subdomain resolution, but the general mechanism (arbitrary custom domains, cert issuance per onboarded domain) isn't even designed yet. Without a note, a team could read the walking skeleton as requiring the full multi-tenant routing layer on day one, ballooning what "thin end-to-end slice" (per `org.md`'s Walking Skeleton practice) was meant to be.

3. **FR5.4 (POI/locality many-to-many) — story-level implication.** Checked every AC that references POIs by locality (US1.6's AC1.6.1, US2.2's AC2.2.1, US2.3's AC2.3.1, US4.1's AC4.1.1/AC4.1.2) for an implicit one-to-one assumption. Most are fine: "browse curated POIs for my locality," "POIs belonging to my property's locality," etc. all scope by locality without asserting exclusivity, which is compatible with FR5.4's many-to-many model. One AC does carry an implicit one-to-one assumption: AC4.1.2 ("Given I submit invalid or duplicate POI data... I see a clear validation error and the entry is not persisted") doesn't scope what "duplicate" means. Under FR5.4, the same real-world POI can legitimately be curated under a second, nearby locality-brand (the boundary-adjacent-attraction case FR5.4 itself gives as an example) — if "duplicate" is checked globally rather than within-locality, an Ops user correctly associating an existing POI with a second locality would be incorrectly rejected.

4. **traceability.json's new FR9/NFR6 rows.** FR9's target list (US4.4, US1.1, US1.5, US2.2, US2.1) covers FR9.1 (US4.4/AC4.4.1), FR9.2 (US2.2/AC2.2.3), FR9.3 (US1.5/AC1.5.3), FR9.4 (US4.4/AC4.4.1), FR9.5 (US4.4/AC4.4.2), and FR9.6 (US1.1/AC1.1.4, tagged inline). FR9.7 (locality-brand fixed after signup, no reassignment) has no dedicated AC, but that's consistent with FR9.7 itself stating reassignment is out of scope — there's no story that would contradict it, so the absence isn't a gap. NFR6's row ("Deferred… domain-design/infrastructure-design (multi-tenant domain routing mechanics)") is accurate and is the same deferral that underlies point 2 above.

## Positions

- AGREE: US1.1's new dependency on US4.4 and US4.4's own framing correctly and consistently express the same precondition in both directions; no change needed.
- OBJECT: Add a note (in the style of the existing US1.4/US2.3/US4.2 notes) to US2.1 and/or US1.1 flagging that AC1.1.4/AC2.1.4 fall inside the walking-skeleton slice while the domain-resolution mechanism they depend on (NFR6.1/NFR6.2) is deferred to domain-design/infrastructure-design — domain-design should explicitly scope how much multi-domain routing the walking-skeleton Bolt needs (e.g., one locality on one subdomain is enough to prove the path; arbitrary custom-domain + TLS provisioning can be a later Bolt) so Bolt 1 doesn't implicitly require the general-purpose routing layer.
- OBJECT: AC4.1.2's "duplicate POI data" check should be scoped explicitly to "duplicate within this locality's curated set," given FR5.4's many-to-many POI-to-locality model — as worded, it risks rejecting a legitimate cross-locality association of an already-curated POI as a duplicate.
- AGREE: No AC otherwise assumes a POI belongs to exactly one locality; the locality-scoped phrasing in US1.6/US2.2/US2.3 is compatible with FR5.4 as written.
- AGREE: traceability.json's new FR9 and NFR6 rows accurately reflect the delta and correctly keep NFR6's domain-routing mechanics deferred rather than asserting a story-level implementation.
